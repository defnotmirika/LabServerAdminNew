<Window x:Class="LabServerAdmin.MaintenanceModeDialog"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
Title="Enable Maintenance Mode" Height="420" Width="480"
WindowStartupLocation="CenterOwner"
ResizeMode="CanResize"
MinHeight="380"
MinWidth="450"
FontFamily="Segoe UI Emoji, Segoe UI, Arial">
<Window.Resources>
    <Style x:Key="ModernButton" TargetType="Button">
        <Setter Property="Background" Value="#050d60"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="15,8"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="3">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#0a1580"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#030a40"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DangerButton" TargetType="Button">
        <Setter Property="Background" Value="#dc3545"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="15,8"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="3">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#c82333"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#bd2130"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</Window.Resources>

<Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Scrollable Content Area -->
    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="15,15,15,10">
        <StackPanel>
            <!-- Header -->
            <TextBlock Text="Enable Maintenance Mode" 
                       FontSize="18" FontWeight="Bold" Foreground="#050d60" 
                       Margin="0,0,0,15"/>

            <!-- Form Fields -->
            <TextBlock Text="Maintenance Message:" FontWeight="SemiBold" Foreground="#050d60" Margin="0,0,0,8"/>
            <TextBox x:Name="MaintenanceMessageTextBox" 
                     Height="90" 
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     FontSize="13" 
                     Padding="8"
                     BorderBrush="#050d60"
                     BorderThickness="2"
                     Text="This computer is currently undergoing maintenance.&#x0a;Please use another computer.&#x0a;&#x0a;Thank you for your patience."/>
                
            <!-- Warning Box -->
            <Border Background="#fff3cd" 
                    BorderBrush="#ffc107" 
                    BorderThickness="2" 
                    CornerRadius="4" 
                    Padding="12"
                    Margin="0,12,0,0">
                <StackPanel>
                    <TextBlock Text="Warning!" 
                               FontWeight="Bold" 
                               FontSize="13" 
                               Foreground="#856404"
                               Margin="0,0,0,6"/>
                    <TextBlock TextWrapping="Wrap" 
                               FontSize="11" 
                               Foreground="#856404"
                               LineHeight="18"
                               Text="The client PC will be locked with this message and cannot be used until you disable maintenance mode from the Computers tab."/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>

    <!-- Action Buttons (Fixed at bottom) -->
    <Border Grid.Row="1" BorderBrush="#CCCCCC" BorderThickness="0,1,0,0" Background="#F9F9F9" Padding="15,10">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="EnableButton" Content="Enable Maintenance" 
                    Style="{StaticResource ModernButton}" Click="EnableButton_Click" 
                    Width="170" Height="36"/>
            <Button x:Name="CancelButton" Content="Cancel" 
                    Style="{StaticResource DangerButton}" Click="CancelButton_Click" 
                    Width="100" Height="36"/>
        </StackPanel>
    </Border>
</Grid>
</Window>
