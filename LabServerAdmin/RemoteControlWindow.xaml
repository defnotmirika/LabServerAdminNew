<Window x:Class="LabServerAdmin.RemoteControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Remote Client Control" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#f5f5f5">

    <Window.Resources>
        <!-- Flat Button Style -->
        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="Background" Value="#050d60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Focusable="False"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0a1580"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#030a40"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#cccccc"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GoldToggle" TargetType="ToggleButton">
            <Setter Property="Background" Value="#f8b800"/>
            <Setter Property="Foreground" Value="#050d60"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Focusable="False"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#ffc61a"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#e0a500"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="border" Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#28a745"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Bar -->
        <Border Grid.Row="0" Background="#050d60" Padding="15,10">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="HeaderText" Text="??? Remote Control - " FontSize="16" FontWeight="Bold" Foreground="#f8b800" VerticalAlignment="Center"/>
                    <TextBlock x:Name="StatusText" Text="Waiting for stream..." FontSize="12" Foreground="White" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <StackPanel Orientation="Horizontal" Margin="0,0,15,0" VerticalAlignment="Center">
                        <TextBlock Text="Refresh rate:" FontSize="12" Foreground="White" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
                        <ComboBox x:Name="RefreshRateComboBox" Height="26" Width="100" SelectionChanged="RefreshRateComboBox_SelectionChanged"
                                  Background="White" BorderBrush="#f8b800" BorderThickness="2">
                            <ComboBoxItem Content="1 FPS" Tag="1000"/>
                            <ComboBoxItem Content="2 FPS" Tag="500"/>
                            <ComboBoxItem Content="5 FPS" Tag="200"/>
                        </ComboBox>
                    </StackPanel>

                    <ToggleButton x:Name="ControlToggleButton" Content="?? Enable Control" Style="{StaticResource GoldToggle}" Width="130"
                                  VerticalAlignment="Center" Checked="ControlToggleButton_Checked" Unchecked="ControlToggleButton_Unchecked"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Screen Display Area -->
        <Border Grid.Row="1" Margin="10" Background="#1a1a1a" CornerRadius="5" BorderBrush="#050d60" BorderThickness="2">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel"
                          Background="#1a1a1a">
                <Image x:Name="ScreenImage" Stretch="Uniform" Focusable="True" FocusVisualStyle="{x:Null}" Cursor="Cross"
                       MouseMove="ScreenImage_MouseMove"
                       MouseLeftButtonDown="ScreenImage_MouseLeftButtonDown"
                       MouseLeftButtonUp="ScreenImage_MouseLeftButtonUp"
                       MouseRightButtonDown="ScreenImage_MouseRightButtonDown"
                       MouseRightButtonUp="ScreenImage_MouseRightButtonUp"
                       MouseWheel="ScreenImage_MouseWheel"
                       Margin="5"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>

