<Window x:Class="LabServerClient.ClientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabServerClient"
        mc:Ignorable="d"
        Title="Lab Server Client" Height="344" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    
    <Window.Resources>
        <Style x:Key="StatusConnected" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Green"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        
        <Style x:Key="StatusDisconnected" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Red"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Client Configuration" FontSize="15" FontWeight="Bold" 
                       HorizontalAlignment="Left"/>
            <Button Grid.Column="1" x:Name="LogoutButton" Content="🚪 Logout" Height="30" Padding="12,6" Margin="10,0,0,0"
                    Click="LogoutButton_Click"/>
        </Grid>

        <!-- Connection Status -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Status:" FontWeight="SemiBold" Margin="0,0,10,0"/>
            <TextBlock x:Name="StatusText" Text="Disconnected" Style="{StaticResource StatusDisconnected}"/>
        </StackPanel>

        <!-- Server Configuration -->
        <GroupBox Grid.Row="2" Header="Server Configuration" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Server IP:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="ServerIpTextBox" Grid.Row="0" Grid.Column="1" Text="192.168.1.100" Margin="0,0,10,0"/>
                <Button x:Name="ConnectButton" Grid.Row="0" Grid.Column="2" Content="Connect" 
                        Click="ConnectButton_Click" Padding="10,5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="PC Name:" VerticalAlignment="Center" Margin="0,10,10,0"/>
                <TextBox x:Name="PcNameTextBox" Grid.Row="1" Grid.Column="1" Text="PC 1" Margin="0,10,10,0"/>
                <Button x:Name="DisconnectButton" Grid.Row="1" Grid.Column="2" Content="Disconnect" 
                        Click="DisconnectButton_Click" Padding="10,5" Margin="0,10,0,0" IsEnabled="False"/>
            </Grid>
        </GroupBox>

        <!-- Current Command -->
        <GroupBox Grid.Row="3" Header="Current Command" Margin="0,0,0,10">
            <StackPanel Margin="10">
                <TextBlock x:Name="CurrentCommandText" Text="None" FontStyle="Italic"
                           HorizontalAlignment="Center" Foreground="Gray"/>
                <TextBlock x:Name="UsageLimitStatusText" Text="Usage limit: none" Margin="0,10,0,0"
                           HorizontalAlignment="Center" Foreground="Gray"/>
            </StackPanel>
        </GroupBox>

        <!-- Logs -->
        <GroupBox Grid.Row="4" Header="Activity Log">
            <ScrollViewer>
                <TextBlock x:Name="LogTextBlock" TextWrapping="Wrap" Margin="10" 
                           FontFamily="Consolas" FontSize="10"/>
            </ScrollViewer>
        </GroupBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="5" Background="#F5F5F5">
            <StatusBarItem>
                <TextBlock x:Name="StatusBarText" Text="Ready"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="LastResponseText" Text="Last Response: Never"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
