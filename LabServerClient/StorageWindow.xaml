<Window x:Class="LabServerClient.StorageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="My Storage"
        Height="520"
        Width="720"
        WindowStartupLocation="CenterOwner"
        Background="#050d60"
        AllowDrop="True"
        DragOver="Window_DragOver"
        Drop="Window_Drop">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#050d60" />
        <SolidColorBrush x:Key="AccentBrush" Color="#f8b800" />
        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#ffd24d" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#e0aa00" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.6" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="12">
        <Border Background="White" CornerRadius="10" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="My Storage" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" />
                    <TextBlock x:Name="OwnerText" Grid.Column="1" FontSize="12" Foreground="{StaticResource PrimaryBrush}" Margin="10,0,0,0" HorizontalAlignment="Right"/>
                </Grid>

                <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,10,0,10" HorizontalAlignment="Left">
                    <Button Content="Upload Files" Width="120" Style="{StaticResource AccentButton}" Margin="0,0,12,0" Click="UploadButton_Click"/>
                    <Button Content="🔄" Width="40" Style="{StaticResource AccentButton}" Margin="0,0,12,0" Click="RefreshButton_Click" ToolTip="Refresh"/>
                    <TextBlock Text="Drag and drop files to upload" VerticalAlignment="Center" Foreground="{StaticResource PrimaryBrush}" Margin="12,0,0,0"/>
                </StackPanel>

                <DataGrid x:Name="FilesGrid" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False"
                          ItemsSource="{Binding Files}" IsReadOnly="True" HeadersVisibility="Column"
                          SelectionMode="Single" Margin="0,0,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Filename" Binding="{Binding FileName}" Width="2*"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding FileType}" Width="*"/>
                        <DataGridTextColumn Header="Size" Binding="{Binding FormattedSize}" Width="*"/>
                        <DataGridTextColumn Header="Uploaded" Binding="{Binding UploadDate, StringFormat=MM/dd/yyyy HH:mm}" Width="*"/>
                        <DataGridTemplateColumn Header="Actions" Width="180">
                            <DataGridTemplateColumn.HeaderTemplate>
                                <DataTemplate/>
                            </DataGridTemplateColumn.HeaderTemplate>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <StackPanel.Resources>
                                            <Style TargetType="Button">
                                                <Setter Property="Margin" Value="0,0,6,0" />
                                            </Style>
                                        </StackPanel.Resources>
                                        <Button Content="Download" Padding="6,2" Click="DownloadButton_Click" Tag="{Binding}" />
                                        <Button Content="Rename" Padding="6,2" Click="RenameButton_Click" Tag="{Binding}" />
                                        <Button Content="Delete" Padding="6,2" Background="#E53935" Foreground="White" BorderThickness="0" Click="DeleteButton_Click" Tag="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
